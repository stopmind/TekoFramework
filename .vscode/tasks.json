{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "meson compile",
            "type": "shell",
            "args": [],
            "problemMatcher": [
                "$tsc"
            ],
            "presentation": {
                "reveal": "always"
            },
            "group": "build",
            "options": {
                "cwd": "${workspaceRoot}/build"
            }
        }
    ]
}